// /// <reference types="cypress" />
// // Cypress is installed by using npm i cypress
// // Cypress is runned by using the npx cypress open

// // visited the project URL by using cy.visit
// context('Actions', () => {
//   beforeEach(() => {
//     cy.visit('http://localhost:3000')
//   })
//   // Tested the perticular functionality by targeting the components and elements 
//     it('.type() - type into a DOM element', () => {
//         cy.get(':nth-child(2) > .btnAccess').click()
//         cy.get('.btnBack').click()
//         cy.get(':nth-child(3) > .btnAccess').click()
//         cy.get(':nth-child(7) > .buttonalbm').click()
//         cy.get('.backAlbam').click()
//         cy.get('.btnBack').click()
        
//         cy.get(':nth-child(9) > .btnAccess').click()
//         cy.get(':nth-child(10) > .buttonalbm').click()

//     })

// })
